# Database
DATABASE_URL=postgres://utrustning:utrustning@localhost:5432/utrustning?sslmode=disable

# API server
ADDR=:8080

# JWT / OIDC
JWKS_URL=https://dev.id.scouterna.se/realms/scoutnet/protocol/openid-connect/certs

# Role mapping
ROLE_MAPPING_PATH=role-mapping.json

# Dev mode
DEV_MODE=true
DEV_PERSONAS_PATH=dev-personas.json

# SvelteKit
ORIGIN=http://localhost:3000
API_URL=http://localhost:8080
AUTH_SECRET=change-me-in-production
AUTH_KEYCLOAK_ID=ms-utrustning
AUTH_KEYCLOAK_SECRET=change-me
AUTH_KEYCLOAK_ISSUER=https://dev.id.scouterna.se/realms/scoutnet
